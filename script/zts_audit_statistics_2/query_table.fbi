#LastModifyDate:　2022-05-07T10:25:37.741578
#FBI脚本文件

#保存语句
#@data_key  条件信息
#@link 链接
#@table 表名

#构建唯一工作区，处理并发
use @FID

#获取表单中的条件信息
a = load ssdb by ssdb0 with @data_key

zts_sja = @udf RS.load_mysql_sql with (mysql1,select uuid from middle_biao)
app = eval zts_sja by iloc[0,0]
a = @udf a by udf0.df_append with (app,$app,like)
#保存
b,c = @udf a by CRUD.query_table with (@link,@table)

assert find_df('b',ptree) as  alert with 数据查询失败！

push b as table
push c as count
#清除工作区
clear @FID
